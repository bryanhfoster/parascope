define(["app/communicationManager"],
function (communicationManager) {  
   var me = {
        viewModel: kendo.observable({ 
            //this is not used and I lazily added to route controller
        })
   };
    
    return me;
});